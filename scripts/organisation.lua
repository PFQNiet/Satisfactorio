return {
	require(modpath.."scripts.organisation.storage-container"),
	require(modpath.."scripts.organisation.industrial-storage-container"),
	require(modpath.."scripts.organisation.beacon"),
	require(modpath.."scripts.organisation.map-marker"),
	require(modpath.."scripts.organisation.object-scanner"),
	require(modpath.."scripts.organisation.lookout-tower")
}
